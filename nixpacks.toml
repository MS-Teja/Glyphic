[providers]
node = true

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["corepack enable", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm turbo build --filter @glyphic/api"]

[start]
cmd = "node apps/api/dist/index.js"
